# FILE: configs/eval_residual_config.yaml
# Configuration for evaluating residual policy

# BC checkpoint (base policy)
bc_checkpoint: "/content/drive/MyDrive/pda/bc/bc_backup_epoch_088.ckpt"

# RL-trained residual checkpoint (optional - leave empty for BC-only baseline)
residual_checkpoint: "/content/dgpo/outputs/2025-12-10/01-29-22/residual_policy_iter_40.pt"  # e.g., "outputs/best_residual_policy.pt"

# Environment
env:
  xml_path: "envs/panda_pick_place.xml"

# Evaluation
num_episodes: 10
max_steps: 200
seed: 42
