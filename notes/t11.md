Comprehensive Debugging Summary
What IS Correctly Configured
Component	Status	Evidence
Vision Backbone	✅ Last 3 layers UNFROZEN	
semantic_planner.py
 L125-148
DAgger Config	✅ freeze_vision: false	
train_dagger_config.yaml
 L9
Training Data	✅ Has expert_target_pose	
semantic_planner_dataset.py
 L99-105
Goal Image (Train)	✅ Uses last frame	
semantic_planner_dataset.py
 L204
What We've Already Ruled Out
Frozen backbone → Last 3 layers ARE trainable
Wrong targets → Dataset has expert_target_pose
Goal image mismatch → Eval scripts do need fixing, but not primary cause
Remaining Hypotheses
Hypothesis 1: Insufficient Training Iterations
Evidence:

DAgger iter 11 still shows hovering
11 iterations × 1024 steps = ~11k samples
Vision backbone fine-tuning typically needs 100k+ samples
Test: Run DAgger for 50+ iterations

Hypothesis 2: Learning Rate Too Low for Backbone
Current: lr: 1.0e-5

Issue: With only 11 iterations, the unfrozen layers may not have updated enough.

Fix: Try lr: 5.0e-5 for backbone layers specifically

Hypothesis 3: Transformer Fusion Not Learning Task-Relevant Features
Architecture Review:

Input: [TrajQ, GripQ, PhaseQ, Proprio, Prev, Curr, Goal]
       (3 query tokens + 1 proprio + 196×3 image tokens)
       = 592 tokens total
Issue: The transformer may be learning to attend primarily to 
proprio
 (position info) and ignoring vision entirely because proprio is "easier" than learning visual features.

Test: Mask proprio input and see if model still predicts same output

Hypothesis 4: Positional Information Leakage
In Dataset:

current_proprio_np = all_proprio[timestep_t].copy()  # Contains EE position!
Issue: 
proprio
 includes current EE position. Model can "cheat" by predicting:

output_position ≈ input_proprio_position + small_delta
This is exactly what we see - the policy hovers around current position.

Evidence from CSV:

policy_pose_z ≈ ee_z + small_noise
Model learned: "predict current pose" (path of least resistance)
Most Likely Root Cause
HYPOTHESIS 4: Positional Information Leakage

The model is taking a shortcut:

proprio
 contains current EE position
Model learns: output = proprio[:3] + noise
This minimizes loss during training (target is close to current pos due to tethered expert)
But fails in closed-loop because it just maintains current pose
Recommended Fix
Option A: Remove EE position from proprio

# Before: proprio = [ee_pos(3), ee_quat(4), joint_pos(7), joint_vel(7), gripper(1)]
# After:  proprio = [ee_quat(4), joint_pos(7), joint_vel(7), gripper(1)]
#                   (Remove first 3 elements)
Option B: Add strong noise to EE position in proprio

# Force model to rely on vision for position
proprio[:3] += np.random.normal(0, 0.1, size=3)  # 10cm noise
Option C: Use state-based goal conditioning

Pass 
goal_pos
 directly as input
Model learns: move from A to B
No shortcut possible
Next Steps
Verify Hypothesis 4: Run quick test with proprio masked
Implement Fix: Either remove EE pos from proprio or add goal_pos input
Retrain: 20+ iterations with fixed architecture
Re-evaluate: Check if policy_pose now varies with object/goal positions